{
  "name": "zora",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "agents"
  ],
  "version": "1.0.0",
  "scripts": {
    "dev:web": "npm run dev --workspaces=apps/web",
    "dev:api": "npm run start:dev --workspaces=apps/api",
    "dev": "concurrently \"npm run dev:web\" \"npm run dev:api\"",
    "build": "npm run build --workspaces"
  },
  "license": "ISC",
  "type": "module",
  "devDependencies": {
  }
}